<Window x:Class="VisorSingularity.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
        xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
        mc:Ignorable="d"
        Title="Wold Virtual p2p 3D" Height="950" Width="1600"
        WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="False"
        ResizeMode="NoResize"
        Foreground="#F8FAFC" FontFamily="Segoe UI"
        MouseDown="Window_MouseDown">

    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#030712" Offset="0.0"/>
            <GradientStop Color="#0B132A" Offset="0.5"/>
            <GradientStop Color="#020617" Offset="1.0"/>
        </LinearGradientBrush>
    </Window.Background>

    <Grid>
        <!-- Real UI Rows -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Real Header -->
            <RowDefinition Height="*"/>    <!-- Real Content (Wizard OR full screen Godot) -->
            <RowDefinition Height="Auto"/> <!-- Real Footer -->
        </Grid.RowDefinitions>

        <!-- ==================== REAL HEADER ==================== -->
        <Border Grid.Row="0" Background="#F9030712" BorderBrush="#20FFFFFF" BorderThickness="0,0,0,1" Height="70">
            <Grid Margin="30,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Logo & Title -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="#1A00E5FF" BorderBrush="#00E5FF" BorderThickness="1" CornerRadius="8" Padding="10,6" Margin="0,0,15,0">
                        <TextBlock Text="⬢" Foreground="#00E5FF" FontSize="22" FontWeight="Bold" VerticalAlignment="Center">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="#00E5FF" BlurRadius="10" ShadowDepth="0"/>
                            </TextBlock.Effect>
                        </TextBlock>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="WOLD VIRTUAL" Foreground="#F8FAFC" FontSize="18" FontWeight="Black"/>
                        <TextBlock Text="P2P 3D VISOR" Foreground="#00E5FF" FontSize="11" FontWeight="Bold"/>
                    </StackPanel>
                </StackPanel>

                <!-- Navigation menu links -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,30,0">
                    <TextBlock Text="ARCHIVO" Foreground="#94A3B8" FontSize="12" FontWeight="SemiBold" Margin="15,0" Cursor="Hand"/>
                    <TextBlock Text="EDITAR" Foreground="#94A3B8" FontSize="12" FontWeight="SemiBold" Margin="15,0" Cursor="Hand"/>
                    <TextBlock Text="AYUDA" Foreground="#94A3B8" FontSize="12" FontWeight="SemiBold" Margin="15,0" Cursor="Hand"/>
                    <TextBlock Text="ADVANCED" Foreground="#94A3B8" FontSize="12" FontWeight="SemiBold" Margin="15,0" Cursor="Hand"/>
                </StackPanel>

                <!-- Custom Window Controls -->
                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="—" Background="#1AFFFFFF" Foreground="#F8FAFC" BorderThickness="0" Width="36" Height="36" Margin="0,0,10,0" Cursor="Hand" Click="BtnMinimize_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="btnBorder" Background="{TemplateBinding Background}" CornerRadius="18">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="btnBorder" Property="Background" Value="#33FFFFFF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Button Content="✕" Background="#1AFF007F" Foreground="#FF007F" BorderThickness="0" Width="36" Height="36" FontWeight="Bold" Cursor="Hand" Click="BtnClose_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="btnBorder" Background="{TemplateBinding Background}" CornerRadius="18">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="btnBorder" Property="Background" Value="#FF007F"/>
                                        <Setter Property="Foreground" Value="#FFFFFF"/>
                                        <Setter TargetName="btnBorder" Property="Effect" Value="{StaticResource GlowMagenta}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ==================== REAL CONTENT ==================== -->
        <Grid Grid.Row="1">
            <!-- Wizard Container -->
            <Grid x:Name="WizardContainer" Visibility="Visible">
                <TabControl x:Name="WizardTabControl" SelectedIndex="0" BorderThickness="0" Background="Transparent">
                    <TabControl.Resources>
                        <Style TargetType="TabPanel">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </Style>
                    </TabControl.Resources>
                    <TabItem Header="Step1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Text="Paso 1: Registro de PC y Firma Digital" Style="{StaticResource CyberLabel}" FontSize="24" HorizontalAlignment="Center" Margin="0,20,0,30"/>

                            <StackPanel Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="400">
                                <TextBlock Text="Nombre de Usuario" Style="{StaticResource CyberLabel}"/>
                                <TextBox x:Name="TxtUsername" Style="{StaticResource CyberTextBox}" Margin="0,0,0,20"/>

                                <TextBlock Text="Firma de Hardware (Fingerprint)" Style="{StaticResource CyberLabel}"/>
                                <TextBox x:Name="TxtHardwareFingerprint" Style="{StaticResource CyberTextBox}" IsReadOnly="True" Text="Generando..." Margin="0,0,0,20"/>

                                <TextBlock Text="UUID de Recuperación" Style="{StaticResource CyberLabel}"/>
                                <TextBox x:Name="TxtUuid" Style="{StaticResource CyberTextBox}" IsReadOnly="True" Text="Generando..." Margin="0,0,0,20"/>

                                <Button x:Name="BtnGenerateSignature" Content="Generar y Descargar Firma Digital (.zip)" Style="{StaticResource EmeraldButton}" Margin="0,0,0,20" Click="BtnGenerateSignature_Click"/>
                            </StackPanel>

                            <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                                <Button x:Name="BtnStep1Next" Content="Siguiente" Style="{StaticResource CyberButton}" IsEnabled="False" Click="BtnStep1Next_Click"/>
                            </StackPanel>
                        </Grid>
                    </TabItem>
                    <TabItem Header="Step2">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Text="Paso 2: Validación de MetaMask" Style="{StaticResource CyberLabel}" FontSize="24" HorizontalAlignment="Center" Margin="0,20,0,30"/>

                            <StackPanel Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="400">
                                <TextBlock Text="Tu Cartera MetaMask" Style="{StaticResource CyberLabel}"/>
                                <TextBox x:Name="TxtWalletAddress" Style="{StaticResource CyberTextBox}" IsReadOnly="True" Text="Esperando conexión..." Margin="0,0,0,20"/>

                                <Button x:Name="BtnConnectMetaMask" Content="Conectar con MetaMask" Style="{StaticResource EmeraldButton}" Margin="0,0,0,20" Click="BtnConnectMetaMask_Click"/>

                                <!-- HTTP Bridge Wait Overlay -->
                                <Border x:Name="PanMetaMaskWaitOverlay" Visibility="Collapsed" Style="{StaticResource GlassPanel}" Padding="20" CornerRadius="12" Margin="0,20,0,0">
                                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                                        <TextBlock Text="Esperando confirmación de MetaMask..." Foreground="#00E5FF" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                                        <ProgressBar IsIndeterminate="True" Height="10" Width="200" Margin="0,10,0,0" Foreground="#00E5FF"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>

                            <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                                <Button x:Name="BtnStep2Back" Content="Anterior" Style="{StaticResource CyberButton}" Margin="0,0,10,0" Click="BtnStep2Back_Click"/>
                                <Button x:Name="BtnStep2Next" Content="Siguiente" Style="{StaticResource CyberButton}" IsEnabled="False" Click="BtnStep2Next_Click"/>
                            </StackPanel>
                        </Grid>
                    </TabItem>
                    <TabItem Header="Step3">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Text="Paso 3: Asignación de Coordenadas de Islas" Style="{StaticResource CyberLabel}" FontSize="24" HorizontalAlignment="Center" Margin="0,20,0,30"/>

                            <StackPanel Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="400">
                                <TextBlock Text="Coordenadas de la Isla (X:Y:Z)" Style="{StaticResource CyberLabel}"/>
                                <TextBox x:Name="TxtIslandCoordinates" Style="{StaticResource CyberTextBox}" Text="0:0:0" Margin="0,0,0,20"/>

                                <TextBlock Text="Nombre de la Isla" Style="{StaticResource CyberLabel}"/>
                                <TextBox x:Name="TxtIslandName" Style="{StaticResource CyberTextBox}" Text="Mi Isla" Margin="0,0,0,20"/>

                                <Button x:Name="BtnSelectIsland" Content="Seleccionar Isla Aleatoria" Style="{StaticResource EmeraldButton}" Margin="0,0,0,20" Click="BtnSelectIsland_Click"/>
                            </StackPanel>

                            <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                                <Button x:Name="BtnStep3Back" Content="Anterior" Style="{StaticResource CyberButton}" Margin="0,0,10,0" Click="BtnStep3Back_Click"/>
                                <Button x:Name="BtnStep3Next" Content="Siguiente" Style="{StaticResource CyberButton}" Click="BtnStep3Next_Click"/>
                            </StackPanel>
                        </Grid>
                    </TabItem>
                    <TabItem Header="Step4">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Text="Paso 4: Confirmación y Entrada al Metaverso" Style="{StaticResource CyberLabel}" FontSize="24" HorizontalAlignment="Center" Margin="0,20,0,30"/>

                            <StackPanel Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="400">
                                <TextBlock Text="¡Registro Completado!" Foreground="#00FF8C" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                <TextBlock Text="Estás listo para entrar al Wold Virtual." Foreground="#F8FAFC" FontSize="14" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,30"/>

                                <TextBlock Text="Nombre de Usuario:" Style="{StaticResource CyberLabel}"/>
                                <TextBlock x:Name="TxtConfirmUsername" Text="[Usuario]" Foreground="#F8FAFC" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>

                                <TextBlock Text="Cartera MetaMask:" Style="{StaticResource CyberLabel}"/>
                                <TextBlock x:Name="TxtConfirmWallet" Text="[Cartera]" Foreground="#F8FAFC" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>

                                <TextBlock Text="Coordenadas de Isla:" Style="{StaticResource CyberLabel}"/>
                                <TextBlock x:Name="TxtConfirmIsland" Text="[Isla]" Foreground="#F8FAFC" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,30"/>

                                <Button x:Name="BtnEnterMetaverse" Content="Entrar al Metaverso" Style="{StaticResource EmeraldButton}" Click="BtnEnterMetaverse_Click"/>
                            </StackPanel>

                            <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                                <Button x:Name="BtnStep4Back" Content="Anterior" Style="{StaticResource CyberButton}" Margin="0,0,10,0" Click="BtnStep4Back_Click"/>
                            </StackPanel>
                        </Grid>
                    </TabItem>
                </TabControl>
            </Grid>

            <!-- Godot Viewport Container -->
            <Grid x:Name="PanViewportContainer" Visibility="Collapsed" Background="#000000" SizeChanged="PanViewportContainer_SizeChanged">
                <wfi:WindowsFormsHost x:Name="GodotHost" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            </Grid>
        </Grid>

        <!-- ==================== REAL FOOTER ==================== -->
        <Border Grid.Row="2" Background="#F9030712" BorderBrush="#20FFFFFF" BorderThickness="0,1,0,0" Height="45">
            <Grid Margin="30,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" x:Name="TxtFooterStatus" Text="Listo para iniciar el proceso de enlace seguro en la red Wold." 
                           Foreground="#94A3B8" FontSize="12" FontWeight="Medium" VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="TxtFooterHwSignature" Text="ENLACE SEGURO FINGERPRINT: ..." Foreground="#00FF8C" FontSize="11" FontWeight="SemiBold" FontFamily="Consolas" Margin="0,0,30,0" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse Width="6" Height="6" Fill="#00E5FF" Margin="0,0,6,0">
                            <Ellipse.Effect><DropShadowEffect Color="#00E5FF" BlurRadius="4" ShadowDepth="0"/></Ellipse.Effect>
                        </Ellipse>
                        <TextBlock x:Name="TxtFooterBridgeStatus" Text="PUENTE HTTP: ONLINE" Foreground="#00E5FF" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>