<Window x:Class="VisorSingularity.ServidorDescentralizado.MiningRigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VisorSingularity.ServidorDescentralizado"
        mc:Ignorable="d"
        Title="Configuración de Rig de Minería" Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="#1A1A1A" Foreground="#00FF00"
        BorderBrush="#00FF00" BorderThickness="1">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Vincular Rig de Minería" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <TextBlock Text="Dirección IP del Rig:" Margin="0,0,0,5"/>
            <TextBox x:Name="IpAddressTextBox" Style="{StaticResource CyberpunkTextBox}" Margin="0,0,0,10"/>

            <TextBlock Text="Puerto de Conexión:" Margin="0,0,0,5"/>
            <TextBox x:Name="PortTextBox" Style="{StaticResource CyberpunkTextBox}" Margin="0,0,0,10"/>

            <TextBlock Text="Clave de API (Opcional):" Margin="0,0,0,5"/>
            <TextBox x:Name="ApiKeyTextBox" Style="{StaticResource CyberpunkTextBox}" Margin="0,0,0,10"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Vincular" Style="{StaticResource CyberpunkButton}" Click="LinkButton_Click" Margin="0,0,10,0"/>
            <Button Content="Cancelar" Style="{StaticResource CyberpunkButton}" IsCancel="True"/>
        </StackPanel>
    </Grid>
    <Window.Resources>
        <!-- Estilos Cyberpunk para TextBox -->
        <Style x:Key="CyberpunkTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="#00FF00"/>
            <Setter Property="BorderBrush" Value="#00FF00"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="CaretBrush" Value="#00FF00"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilos Cyberpunk para Button -->
        <Style x:Key="CyberpunkButton" TargetType="Button">
            <Setter Property="Background" Value="#008000"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#00FF00"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#00FF00"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#00B300"/>
                    <Setter Property="BorderBrush" Value="#00FF00"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
</Window>
